name: Deploy-Dist
on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: get Code
        run: |
          echo "Hello User"
          echo "${{toJson(github)}}"
          echo git clone https://github.com/${{github.repository}}.git
